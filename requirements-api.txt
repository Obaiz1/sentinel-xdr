# Sentinel XDR — Deep Learning final project: SERVING (FastAPI inference) deps.
# Minimal runtime needed to load the trained model and serve /predict.
# Used by Dockerfile.final-project. Python 3.11 recommended.

tensorflow-cpu==2.16.1
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
scikit-learn==1.5.0
joblib==1.4.2
pandas==2.2.2
numpy==1.26.4
