# Sentinel XDR — Deep Learning final project: FULL environment.
# Superset that covers training + serving + experiment tracking. Use this for a
# single local virtualenv that does everything. Python 3.11 recommended.
#
#   python3.11 -m venv .venv-dl
#   source .venv-dl/bin/activate        # Windows: .venv-dl\Scripts\activate
#   pip install -r requirements-final.txt

tensorflow-cpu==2.16.1
scikit-learn==1.5.0
pandas==2.2.2
numpy==1.26.4
joblib==1.4.2
matplotlib==3.9.0
mlflow==2.14.1
fastapi==0.111.0
uvicorn[standard]==0.30.1
pydantic==2.7.4
requests==2.32.3
